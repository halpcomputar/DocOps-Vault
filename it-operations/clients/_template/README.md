# Client Template

Copy this folder to create a new client workspace.

Recommended folder name:

`it-operations/clients/<client-slug>/`

Use readable slugs instead of private internal short codes. The goal is for the folder name to make sense months later without a lookup table.

